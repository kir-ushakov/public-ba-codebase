/**
 * Changed Entity Enum
 * 
 * Defines which entities can be synchronized in the system.
 */

/**
 * Enum of entities that can be synchronized
 */
export enum EChangedEntity {
  Task = 'CHANGED_ENTITY_TASK',
  Tag = 'CHANGED_ENTITY_TAG',
  // more types will be here
}


