export namespace ProfileScreenAction {
  export class Logout {
    static readonly type = '[ProfileScreen] Logout';
  }

  export class RemoveFromSlack {
    static readonly type = '[ProfileScreen] Remove From Slack';
  }
}
