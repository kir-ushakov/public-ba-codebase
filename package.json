{
  "lint-staged": {
    "frontend/**/*.{ts,js,json}": "npx eslint --config frontend/eslint.config.mjs --no-warn-ignored",
    "backend/**/*.{ts,js,json}": "npx --prefix backend eslint --fix --config backend/eslint.config.js"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.5.1"
  },
  "scripts": {
    "prepare": "husky install"
  }
}
