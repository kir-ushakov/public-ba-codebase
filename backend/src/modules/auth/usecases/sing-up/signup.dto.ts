export type SignUpRequestDTO = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
};

export type SignUpResponseDTO = {
  email: string;
  firstName: string;
  lastName: string;
};
