---
description: Backend tests — integration by default, unit only for pure domain
globs: backend/test/**/*.ts
alwaysApply: false
---

# Backend tests

The default for this backend is **integration**: a real Express app wired to the real controller, use-case, repo and Mongoose models, running against in-memory MongoDB.

Do **not** unit-test a use-case or controller by mocking the repo, mapper, or domain. That is the kind of unit test this project forbids — it re-implements the stack in mocks and misses the bugs that live at the seams.

## When unit tests are allowed

Isolated tests **without** HTTP or Mongo are allowed for **pure** code that has no I/O:

- domain entities and value objects (`Task.create` / `update`, `UserEmail`, `Guard`)
- mappers (`TaskMapper.toDomain` / `toPersistence` / `toDTO`)

These live in `test/unit/`, named `<subject>.spec.ts`. They must not mock neighbouring layers — call the real class. Do not add unit tests for routers, controllers, use-cases, or repos.

## Integration layout and naming

- `test/integration/<module>/` mirrors `src/modules/<module>/usecases/`
- specs are `<use-case>.int.spec.ts`, described as `Integration: CreateTask (Controller -> UseCase -> Repo -> MongoDB)`
- each folder may still have a local `test-app.ts` until migrated; new specs use `test/integration/_setup/build-test-app.ts`
- JWT helpers live in `test/integration/_setup/auth.helper.ts`; env vars in `test/setup-env.ts`

Imports carry the `.js` extension like the rest of the backend (`../_setup/mongo-memory.js`).

## Test app builder

Prefer `buildTestApp()` from `test/integration/_setup/build-test-app.ts`: it calls production `createApp()` (real routers, Passport JWT, multer). Seed a user with `seedTestUser()` and send `Cookie: jwt=...` via `authenticatedRequest()`. Do not invent a parallel mini-Express or fake `req.user` for new tests.

Mock only external systems — Slack, Google Drive, OpenAI, email. Repos, mappers, domain models and the database stay real. Google Drive is stubbed in Jest via `test/__mocks__/google-drive-services.ts` so the ESM `mime` package is never loaded.

## Lifecycle

```ts
beforeAll(async () => {
  await startInMemoryMongo();
  app = buildTestApp().app;
}, 30_000);

afterAll(async () => {
  await stopInMemoryMongo();
});

beforeEach(async () => {
  await clearDatabase();
  jest.clearAllMocks();
});
```

## Assertions

Assert observable outcomes rather than internals: the HTTP status, the response body typed against the DTO from `@brainassistant/contracts`, and the document read back through the model. Cover the happy path, the validation failure (status plus `name` and `message` in the body) and external-service failure.

Specs must be deterministic: no real network, no assertions that depend on the current time or random values, no reliance on execution order. Do not leave `console.log` debugging in a spec.
